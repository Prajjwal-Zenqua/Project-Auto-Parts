<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Auto Parts Seller Portal</title>
<style>
body {
  font-family: 'Arial', sans-serif;
  background-color: #f7f7f7;
  color: #333;
  margin: 0;
  padding: 20px;
}

header {
  background-color: #004aad;
  color: #fff;
  padding: 10px 20px;
  text-align: center;
  font-size: 24px;
}

button {
  background-color: #0084ff;
  border: none;
  color: white;
  padding: 10px 20px;
  text-align: center;
  text-decoration: none;
  display: inline-block;
  font-size: 16px;
  margin: 4px 2px;
  cursor: pointer;
  border-radius: 5px;
}

button:hover {
  background-color: #0056b3;
}

.content {
  padding: 20px;
  text-align: center;
}

.welcome-message {
  font-size: 20px;
  margin-bottom: 20px;
}

</style>
</head>
<body>

<header>
  <% if user_signed_in? %>
    <div class="welcome-message">Welcome <%= current_user.email %></div>
    <%= button_to "Sign out", destroy_user_session_path, method: :delete, form_class: "button" %>
  <% else %>
    <%= button_to "Sign in/ Sign up", new_user_session_path, form_class: "button" %>
  <% end %>
</header>

<div class="content">
  <h1>Hello Auto Parts User!</h1>
</div>

</body>
</html>
